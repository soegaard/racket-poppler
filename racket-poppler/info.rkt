#lang setup/infotab
(define name "racket-poppler")
(define blurb
  '("These bindings for libpoppler you render pdf pages to picts and bitmap%s. You can also gather text and layout information. As a bonus render-tex will render TeX into picts."))
(define primary-file "main.rkt")
(define categories '(media io))
(define repositories '("4.x"))
(define scribblings '(("main.scrbl" ())))
(define release-notes '((p "Initial release")))
